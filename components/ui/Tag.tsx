export function Tag({ children }: { children: React.ReactNode }) {
  return <span className="rounded-full bg-brand-100 px-3 py-1 text-xs uppercase tracking-widest text-brand-600">{children}</span>;
}
